# PeopleDesk
An employee management system built on React and NestJS.

# Running and Testing
- Run `npm start` in both projects to run the api and the client on ports 5000 and 5001 respectively.
- Use admin test credentials of `admin` username and `admin123` password on the client.